{
  "name": "cf-logs-to-telegram",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "wrangler": "^4.75.0",
    "vitest": "4.1.4",
    "@cloudflare/vitest-pool-workers": "^0.13.2"
  }
}