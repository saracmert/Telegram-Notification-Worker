{
  "name": "cf-logs-to-telegram",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "wrangler": "^4.36.0",
    "vitest": "3.2.4",
    "@cloudflare/vitest-pool-workers": "^0.9.0"
  }
}